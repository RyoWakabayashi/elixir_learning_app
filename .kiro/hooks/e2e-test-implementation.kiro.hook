{
  "enabled": true,
  "name": "E2E Test Implementation",
  "description": "Monitors changes to application files and implements E2E tests using PhoenixTest.Playwright to verify screen operations and displays",
  "version": "1",
  "when": {
    "type": "userTriggered",
    "patterns": [
      "lib/elixir_learning_app_web/live/*.ex",
      "lib/elixir_learning_app_web/controllers/*.ex",
      "test/e2e/*.exs"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "I notice changes to application files. Please implement or update E2E tests in the test/e2e directory that:\n\n1. Use PhoenixTest.Playwright to test the current screen\n2. Verify that UI elements are displayed correctly\n3. Test user interactions with the page\n4. Confirm that navigation between pages works as expected\n5. Validate any form submissions and their responses\n\nAfter implementing the tests, run them with:\n```\nMIX_ENV=test mix test\n```\n\nPlease ensure the tests are comprehensive and verify both the visual display and functional behavior of the application."
  }
}